name: Topograph CI

on:
  push:
    paths:
      - "EvoNN-Topograph/**"
      - "shared-benchmarks/**"
      - "MONOREPO.md"
      - "pyproject.toml"
      - "uv.lock"
  pull_request:
    paths:
      - "EvoNN-Topograph/**"
      - "shared-benchmarks/**"
      - "MONOREPO.md"
      - "pyproject.toml"
      - "uv.lock"

jobs:
  topograph:
    runs-on: macos-14
    env:
      TOPOGRAPH_CHECK_SCRIPT: scripts/ci/topograph-checks.sh
      TOPOGRAPH_PACKAGE: topograph
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - uses: astral-sh/setup-uv@v5

      - name: Install Topograph dev dependencies
        run: uv sync --package "${TOPOGRAPH_PACKAGE}" --extra dev

      - name: Ruff
        run: bash "${TOPOGRAPH_CHECK_SCRIPT}" ruff

      - name: Pytest
        run: bash "${TOPOGRAPH_CHECK_SCRIPT}" pytest
