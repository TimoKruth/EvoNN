"""Adapter helpers for legacy parity slots and current sibling projects."""

from evonn_compare.adapters.slots import (
    canonical_slot,
    fallback_native_id,
    system_display_name,
)

__all__ = ["canonical_slot", "fallback_native_id", "system_display_name"]
