name: all_shared_image__digits_image
tier: 1
description: Derived shared pack restricted to image tasks currently supported by
  all three systems. Single-benchmark slice for digits_image.
benchmarks:
- benchmark_id: digits_image
  native_ids:
    evonn: digits_image
    evonn2: digits
    hybrid: digits
  task_kind: classification
  metric_name: accuracy
  metric_direction: max
  comparison_status: supported
  notes: ''
budget_policy:
  evaluation_count: 64
  epochs_per_candidate: 20
  budget_tolerance_pct: 10.0
seed_policy:
  mode: shared
  required: true
