seed: 42
run_name: official_tier1_core_eval64_seed42
runtime:
  backend: auto
benchmark_pool:
  name: tier1_core_eval64
  benchmarks:
    - iris_classification
    - wine_classification
    - breast_cancer
    - moons_classification
    - digits_image
    - diabetes
    - friedman1
    - credit_g
search:
  mode: budget_matched
  target_evaluation_count: 64
training:
  epochs_per_candidate: 20
primitive_pool:
  tabular: [mlp, sparse_mlp, moe_mlp]
  synthetic: [mlp, sparse_mlp]
  image: [mlp, conv2d, lite_conv2d]
  language_modeling: [embedding, attention, sparse_attention]
