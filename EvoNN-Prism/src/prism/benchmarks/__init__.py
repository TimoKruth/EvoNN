"""Benchmark specs, data loading, and parity mappings.

Public helpers live in the sibling modules such as ``datasets``, ``parity``,
``preprocess``, and ``spec``. This package initializer intentionally avoids
re-exporting them to keep imports side-effect free.
"""

from __future__ import annotations

__all__: tuple[str, ...] = ()
